# A股 K 线复盘训练器：Windows EXE 零基础使用说明

> **适用对象**：没有编程经验、拿到已经编译好的 Windows 安装包或 EXE 的用户。<br>
> **不需要**：安装 Python、输入命令或阅读源代码。<br>
> **需要**：Windows 10/11、鼠标键盘；使用在线行情或 AI 时还需要联网。

![软件主界面示例](https://github.com/user-attachments/assets/e516c90f-9312-4811-991f-f0852a7dff58)

## 先用一分钟了解完整流程

```mermaid
flowchart LR
    A[安装并启动] --> B[创建用户]
    B --> C[新建训练]
    C --> D[逐根查看 K 线]
    D --> E[模拟买入/卖出]
    E --> F[结束训练]
    F --> G[阅读复盘报告]
```

第一次使用，建议只做这 6 件事：**安装 → 创建用户 → 新建训练 → 点击“下一根 K 线” → 尝试买卖 → 结束训练看报告**。AI、筹码分布和参数设置都可以熟悉基础操作后再用。

---

## 1. 安装和首次启动

### 1.1 你可能收到两种文件

| 收到的内容 | 怎么做 |
| --- | --- |
| `KLineTrainer_Setup_*.exe` 一类安装包 | 双击安装，按安装向导完成，然后从桌面或开始菜单启动。 |
| `KLineTrainer.exe` 和若干文件/文件夹 | **先完整解压**到一个普通文件夹，再双击 `KLineTrainer.exe`；不要只从压缩包预览窗口运行。 |

如果同时收到 `KLineTrainer_AI_Assistant.exe`，它是可选的 AI 策略测试器。请让它与主程序保持在发行包原本的位置，不要单独拖走；普通手动训练不需要打开它。

### 1.2 Windows 出现安全提示怎么办

只在你确认安装包来自本项目官方发布页或可信发送者时继续：

1. 若出现“Windows 已保护你的电脑”，点击 **更多信息**。
2. 核对应用名称和来源，再点击 **仍要运行**。
3. 若杀毒软件直接拦截，不要盲目关闭安全软件；先重新从可信来源下载并核对发布说明。

### 1.3 启动后的正常现象

- 第一次启动可能比平时慢，请等待窗口出现，不要连续双击。
- 程序会在本机启动一个仅供界面使用的后台服务；这不是在登录证券账户。
- 若提示缺少 **Microsoft Edge WebView2 Runtime**，请从微软官方渠道安装 WebView2 Runtime 后重启程序。Windows 10/11 通常已自带。
- 建议将窗口最大化。界面最低适合在约 1200 × 800 的显示区域使用。

> [!IMPORTANT]
> 本软件是历史行情训练工具，不连接券商、不进行真实委托。界面中的资金、持仓和盈亏都是模拟数据。

---

## 2. 创建你的第一个用户

启动后首先看到“选择用户”页面：

1. 在“输入新用户名”框中填写一个容易识别的名字，例如 `小李`。
2. 点击 **创建新用户**。
3. 以后启动时，直接点击已有用户名即可继续使用。

每个用户分别保存设置、训练记录和统计数据。多人共用电脑时，可以一人创建一个用户。

> [!CAUTION]
> 删除用户会永久删除该用户的设置和训练记录，无法撤销。不要把“删除用户”当作“退出登录”。要换人使用，请点击顶部的 **切换用户**。

---

## 3. 建议先完成一次基础设置

进入主页面后，点击右上角 **设置**。

| 设置项 | 新手建议 | 含义 |
| --- | --- | --- |
| 默认初始资金 | 保持默认值 | 每局开始时的模拟现金。 |
| 佣金费率 / 最低佣金 | 不清楚就保持默认 | 模拟交易手续费。 |
| 印花税率 | 不清楚就保持默认 | 模拟卖出时产生的税费。 |
| 界面主题 | 按喜好选择 | 浅色或深色显示。 |
| 开启 AI 介入 | 第一次先不勾选 | 只有配合 AI 测试器时才需要。 |
| 均线周期 | 保持默认 | 最多显示 6 条移动平均线。 |
| MACD / KDJ / RSI / BOLL | 保持默认 | 技术指标的计算参数。 |

修改后点击 **保存**。如果只是误打开，点击 **取消**，不会保存本次修改。

---

## 4. 开始第一局训练

点击右上角 **新建训练**，选择以下两种模式之一。

### 4.1 指定模式（第一次推荐）

适合想练某只股票的用户：

1. 保持 **指定模式**。
2. 输入 6 位股票代码，例如 `000001`；只填数字，不要填股票名称。
3. 选择起始日期。程序会从历史行情中截取训练区间。
4. 数据源第一次可选择 **AKShare（免费公网）**。
5. 确认初始资金，点击 **开始训练**。

### 4.2 盲盒模式

适合避免“事后知道涨跌”的心理影响：

1. 切换到 **盲盒模式**。
2. 选择板块范围（全部、主板、创业板或中小板）。
3. 填写随机日期范围。
4. 选择数据源并点击 **开始训练**。

### 4.3 数据源怎么选

| 数据源 | 适合谁 | 注意事项 |
| --- | --- | --- |
| AKShare（免费公网） | 大多数首次用户 | 需要联网；网络或上游接口繁忙时可能失败，稍后重试即可。 |
| 本地离线数据 | 已领取或已下载离线数据包的用户 | 不依赖实时网络，但所选股票和日期必须在本地数据范围内。 |
| xtdata / XTQuant | 已安装并配置对应客户端的用户 | 不是开箱即用选项。 |
| mootdx | 熟悉通达信数据源的进阶用户 | 依赖网络和数据源可用性。 |

> [!TIP]
> “开始训练失败”最常见的原因是股票代码错误、所选日期没有足够行情、网络暂时不可用，或本地离线包里没有该股票。先换一个较早的交易日和常见股票代码重试。

---

## 5. 看懂训练界面

![训练界面示例](https://github.com/user-attachments/assets/ef5286a0-8ebc-4c73-81ff-9b518a4b3fea)

训练页面可以分成三块：

| 区域 | 你会看到什么 | 主要用途 |
| --- | --- | --- |
| 左侧 | 总资产、可用资金、持仓市值、浮动盈亏、交易记录 | 查看账户变化，结束或重置训练。 |
| 中间 | K 线、成交量、均线、技术指标 | 观察走势；鼠标移动可查看某根 K 线。 |
| 右侧 | 播放控制、当日详情、当前持仓、交易操作 | 推进日期和模拟买卖。 |

中间图表上方可以切换 **日线 / 周线**；下方技术指标可切换 **MACD、KDJ、RSI、BOLL**。它们只改变观察方式，不会自动替你交易。

### K 线最简读法

- 每根蜡烛代表一个交易周期；日线下通常是一日。
- 红绿颜色请以界面价格和涨跌幅为准，不要只凭颜色下结论。
- “开盘、最高、最低、收盘、成交量”显示在右侧“当日详情”。
- 训练只展示当时已经出现的行情，点击下一根后才会看到下一期数据。

---

## 6. 推进行情并模拟交易

### 6.1 手动推进（新手推荐）

1. 观察当前 K 线和指标。
2. 决定买入、卖出或观望。
3. 点击 **下一根 K 线**，继续下一天。

也可以点击播放按钮自动推进，并在“自动播放”下拉框选择每根 K 线间隔。需要思考或交易时，请先暂停，避免行情继续前进。

### 6.2 买入

1. 在“交易数量（手）”输入数量。A 股通常 **1 手 = 100 股**。
2. 查看界面提示的“最大可买”手数。
3. 点击 **买（收盘）**，按当前 K 线收盘价模拟成交。
4. 若想按开盘价模拟，按住 `Shift`，或点击 `⇧` 切换后再买入。
5. 成交后检查左侧“交易记录”和右侧“当前持仓”。

### 6.3 卖出和 T+1

1. 输入要卖出的手数，并确认不超过“最大可卖”。
2. 点击 **卖（收盘）**；按开盘价卖出的方法与买入相同。
3. A 股实行模拟 **T+1**：当天买入的股票通常当天不能卖，要推进到下一个交易日。

如果按钮没有按预期成交，先检查：交易数量是否大于 0、现金或可卖持仓是否足够，以及是否受到 T+1 限制。

### 6.4 常用快捷键

| 快捷键 | 操作 |
| --- | --- |
| `空格` / `Enter` | 下一根 K 线 |
| `B` / `+` / `=` | 按收盘价买入 |
| `Shift + B` / `Shift + +` / `Shift + =` | 按开盘价买入 |
| `S` / `-` | 按收盘价卖出 |
| `Shift + S` / `Shift + -` | 按开盘价卖出 |
| 数字键 | 输入交易手数 |

快捷键仅在训练界面生效。第一次使用时建议先用按钮，确认数量后再用快捷键。

---

## 7. 结束、重置和复盘

### 7.1 “结束训练”和“重置训练”的区别

- **结束训练**：结算本局并生成复盘报告。如果仍有持仓，程序会询问是否自动平仓；受 T+1 影响时可能自动推进到可卖日期。
- **重置训练**：清空本局交易记录并重新开始，适合误操作后重练；操作前会再次确认。

### 7.2 阅读复盘报告

![复盘报告示例](https://github.com/user-attachments/assets/7910546f-ffba-4213-a6ef-89793450dee5)

报告中重点看：总收益率、最终资产、交易次数、胜率与每笔交易明细。不要只看一次训练的盈亏，建议重复训练并记录“为什么交易”和“是否遵守自己的规则”。

报告底部可执行：

- **查看完整走势**：揭晓之后的完整行情，用于对照当时判断。
- **AI 智能复盘点评**：仅在正确启用并配置 AI 后显示。
- **开始新训练**：直接进入下一局。
- **返回主菜单**：回到主页。

---

## 8. 离线数据和“数据更新器”（可选）

如果发行包附带离线数据，请保留原目录结构。程序使用的离线行情位于程序数据目录下的 `data/a_market_offline`；不要随意改文件名或表头。

通过顶部 **数据更新器** 可以补充数据：

1. 选择单只股票、全沪市、全深市或全市场。
2. 单股模式输入 6 位代码。
3. 选择在线数据源和日期范围。
4. 通常不要勾选“重新全量覆盖”；增量更新更快。
5. 点击 **开始补数**，等待进度结束，不要中途关闭程序。

全市场补数会消耗较长时间和大量网络流量。新手建议先补一只股票，确认流程正常后再扩大范围。

---

## 9. AI 功能（完全可选）

> [!WARNING]
> AI 服务可能按调用量收费，也可能生成错误判断。API Key 等同于密码，不要截图、转发或提交到公开仓库；务必在服务商后台设置额度上限。AI 输出不构成投资建议。

### 9.1 准备工作

1. 确保 `KLineTrainer.exe` 与 `KLineTrainer_AI_Assistant.exe` 使用发行包原有目录结构，以便共享 `data` 目录。
2. 打开主程序，进入 **设置**，勾选 **开启 AI 介入**，点击 **保存**。
3. 在主程序中开始一局训练，并保持主程序运行。
4. 再打开 `KLineTrainer_AI_Assistant.exe`。
5. 在 AI 测试器中填写服务商的 Base URL、API Key、模型名称和自然语言策略，保存后连接主程序。

### 9.2 两种用途

- **AI 策略测试**：AI 测试器读取当前训练数据，给出买、卖、观望或推进指令；自动模式下还会推进 K 线。
- **AI 复盘点评**：结束训练后，在报告页点击 **AI 智能复盘点评**，获取总结、风险提示和改进建议。

如提示“未检测到有效训练会话”，依次检查：主程序是否仍在运行、是否已开始一局训练、AI 介入开关是否已保存、两个 EXE 是否仍处于正确目录结构。

---

## 10. 数据保存、备份与升级

程序通常会在安装/运行目录附近使用以下数据：

| 目录 | 内容 |
| --- | --- |
| `users` | 每位用户的设置、训练历史数据库。 |
| `data` | 行情缓存、离线行情和 AI 配置。 |

建议每隔一段时间备份这两个目录。操作方法：

1. 先退出主程序和 AI 测试器。
2. 将整个 `users` 和 `data` 目录复制到另一个磁盘或备份位置。
3. 升级前也做一次备份；不要在程序运行时覆盖数据库文件。

若程序安装在 `Program Files` 等受保护目录并出现无法保存的问题，请优先使用正式安装包，或将便携版放到自己的文档目录等有写入权限的位置运行。

---

## 11. 常见问题速查

| 现象 | 建议处理顺序 |
| --- | --- |
| 双击没有窗口 | 等待 30 秒 → 在任务管理器结束重复进程 → 重新启动 → 检查 WebView2 与安全软件记录。 |
| 界面空白 | 最大化或重启 → 安装/修复 WebView2 Runtime → 不要移动发行包内文件。 |
| 在线行情加载失败 | 检查网络 → 核对 6 位代码和日期 → 稍后重试 → 换另一个数据源或离线数据。 |
| 离线模式找不到股票 | 检查 `data/a_market_offline` 是否含对应代码，以及日期是否落在数据覆盖范围内。 |
| 买入失败 | 检查资金、手数以及“最大可买”。 |
| 卖出失败 | 检查持仓、“最大可卖”和 T+1 限制。 |
| AI 测试器连不上 | 保持主程序和训练会话开启 → 保存“开启 AI 介入” → 检查两个程序的目录结构。 |
| AI 请求报错 | 核对 Base URL、API Key、模型名、账户余额及网络；不要把完整 Key 发给他人排查。 |
| 换电脑后记录消失 | 从旧电脑恢复备份的 `users` 和 `data` 目录。 |

仍无法解决时，向维护者提供以下信息会更容易定位：Windows 版本、软件版本、操作步骤、完整错误文字，以及**遮盖 API Key 和个人信息后的截图**。

---

## 12. 新手练习清单

- [ ] 成功启动程序并创建用户
- [ ] 保持默认参数开始一局指定模式训练
- [ ] 手动推进 5 根 K 线
- [ ] 买入 1 手并在下一交易日尝试卖出
- [ ] 切换一次日线/周线和技术指标
- [ ] 结束训练并阅读复盘报告
- [ ] 退出程序后重新打开，确认用户与历史记录仍在
- [ ] 需要时再学习离线数据、自动播放和 AI 功能

> **风险提示**：本项目仅供学习、训练和投研交流，不构成任何投资建议。历史表现不代表未来收益，投资有风险，入市需谨慎。
